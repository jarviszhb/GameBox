server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/game_box?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: top.jarviscode.dao 